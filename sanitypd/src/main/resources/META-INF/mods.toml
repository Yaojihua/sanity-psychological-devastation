# This is an example mods.toml file. It contains the data relating to the loading mods.
# There are several mandatory fields (#mandatory), and many more that are optional (#optional).
# The overall format is standard TOML format, v0.5.0.
# Note that there are a couple of TOML lists in this file.
# Find more information on toml format here:  https://github.com/toml-lang/toml
# The name of the mod loader type to load - for regular FML @Mod mods it should be javafml
modLoader="javafml" #mandatory
# A version range to match for said mod loader - for regular FML @Mod it will be the forge version
loaderVersion="[46,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
# The license for you mod. This is mandatory metadata and allows for easier comprehension of your redistributive properties.
# Review your options at https://choosealicense.com/. All rights reserved is the default copyright stance, and is thus the default here.
license="MIT"
# A URL to refer people to when problems occur with this mod
# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
# The modid of the mod
modId="sanitypd" #mandatory
# The version number of the mod - there's a few well known ${} variables useable here or just hardcode it
# ${file.jarVersion} will substitute the value of the Implementation-Version as read from the mod's JAR file metadata
# see the associated build.gradle script for how to populate this completely automatically during a build
version="${file.jarVersion}" #mandatory
 # A display name for the mod
# Kept in English only: Jade renders displayName in entity/block tooltips, where an
# extra CJK suffix is too long. The Chinese name lives in zh_cn.json (itemGroup.sanitypd).
displayName="Sanity: Psychological Devastation" #mandatory
# A URL to query for updates for this mod. See the JSON update specification https://docs.minecraftforge.net/en/latest/misc/updatechecker/
# A URL for the "homepage" for this mod, displayed in the mod UI
# A file name (in the root of the mod JAR) containing a logo for display
logoFile= "sanitypd512.png" #optional
# A text field displayed in the mod UI
credits="Original art/audio: toujourspareil, Zapsplat; based on Sanity: Descent Into Madness by croissantnova (MIT); AI-assisted development: DeepSeek Harness (dsh)" #optional
# A text field displayed in the mod UI
authors="Yaojihua; croissantnova (original Sanity: Descent Into Madness)" #optional
# Display Test controls the display for your mod in the server connection screen
# MATCH_VERSION means that your mod will cause a red X if the versions on client and server differ. This is the default behaviour and should be what you choose if you have server and client elements to your mod.
# IGNORE_SERVER_VERSION means that your mod will not cause a red X if it's present on the server but not on the client. This is what you should use if you're a server only mod.
# IGNORE_ALL_VERSION means that your mod will not cause a red X if it's present on the client or the server. This is a special case and should only be used if your mod has no server component.
# NONE means that no display test is set on your mod. You need to do this yourself, see IExtensionPoint.DisplayTest for more information. You can define any scheme you wish with this value.
# IMPORTANT NOTE: this is NOT an instruction as to which environments (CLIENT or DEDICATED SERVER) your mod loads on. Your mod should load (and maybe do nothing!) whereever it finds itself.
#displayTest="MATCH_VERSION" # MATCH_VERSION is the default if nothing is specified (#optional)

# The description text for the mod (multi line!) (#mandatory)
description='''[ALPHA TEST BUILD] Mechanics, numbers and config keys may still change. Back up your worlds.
[ALPHA 测试版] 机制、数值与配置键仍可能变动，安装前请备份存档。
Builds on the original Sanity mod with new mechanics, items, mobs and commands.
在原版 Sanity 模组的基础上进行了新的机制、物品、生物、命令设计。
Roadmap: this build is the framework. Still to come: gear that strengthens or restricts the mind
(thinking enhancement and self-imposed restrictions), and a final boss.
开发路线：当前版本是骨架。后续将加入思维强化与自限制类装备系统，以及最终 Boss。
Mechanics are derived from Sanity: Descent Into Madness by croissantnova (MIT); the original
author's attribution and license are preserved in full.'''
# GeckoLib is a required dependency.
# GeckoLib and Forge versions must match; a wrong pairing prevents the whole mod from loading:
#   * GeckoLib 4.2           -> works on Forge 46 (the combination this mod is built against)
#   * GeckoLib 4.2.1 ~ 4.8.4 -> its own mods.toml requires Forge [47.1,), so FML rejects it on Forge 46:
#         "Missing language javafml version [47,) wanted by geckolib-...jar, found 46"
#         followed by "Missing or unsupported mandatory dependencies: Mod ID: 'geckolib'"
#   * on Forge 47.1+ use GeckoLib 4.8.4 (also tested against this mod: server starts, self-checks pass)
# The range [4.2,) is accurate for the GeckoLib API itself (this mod is compatible with 4.2~4.8.4);
# whether it can load also depends on the user's Forge version, which is why the pairing is documented
# here instead of narrowing the range (narrowing it would wrongly reject Forge 47 users).
[[dependencies.sanitypd]]
    "modId"="geckolib"
    mandatory=true
    versionRange="[4.2,)"
    side="BOTH"
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.sanitypd]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[46,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.sanitypd]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.20,1.21)"
    ordering="NONE"
    side="BOTH"
# Jade: optional soft dependency. When present, an extra "Sanity: current/max" line is shown
# under the health bar in Jade's tooltip. The mod works fine without it.
[[dependencies.sanitypd]]
    modId="jade"
    mandatory=false
    versionRange="[11,)"
    ordering="AFTER"
    side="BOTH"